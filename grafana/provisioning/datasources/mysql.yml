apiVersion: 1

datasources:
  - name: TradeDB
    uid: trade-mysql
    type: mysql
    url: mysql:3306
    database: tradedb
    user: tradeuser
    secureJsonData:
      password: ${MYSQL_PASSWORD}
    jsonData:
      maxOpenConns: 10
      maxIdleConns: 10
      connMaxLifetime: 14400
      timezone: Asia/Tokyo
    isDefault: true
